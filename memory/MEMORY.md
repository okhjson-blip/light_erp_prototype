# MEMORY (AX-ERP 프로젝트)

§ 프로젝트 정체: AX ERP — 프로토타입(v1~v2) → MVP → Enterprise ERP → AX Platform 단계로 확장 예정. 현재 단계 = 프로토타입.
§ 에이전트 체계: Harness(오케스트레이터) 하에 Planner→Executor→Evaluator 순서 강제. 동일 오류 2회 발생 시 즉시 중단 후 CLAUDE.md에 Constraints 추가.
§ 기술 스택 결정(2026-07-05): 설계문서(2.5/2.8)는 Java Spring Boot+PostgreSQL+K8s를 명시하지만, 프로토타입은 에러/토큰 최소화를 위해 경량 Python(FastAPI+SQLite)으로 진행하기로 사용자 확정. v2 이후 PostgreSQL 전환 검토.
§ v1 범위 확정(2026-07-05): Phase 1 Core 7모듈(MDM/Sales/Procurement/Inventory/Production/FI/Common)만 구현. AI Agent 오케스트레이션 런타임은 이번 범위에서 제외(문서상 v2 이후 Phase).
§ 설계문서 한계: 1.x/2.x 문서는 목표 분량(ERD 150~250 엔티티 등)에 크게 못 미치는 개요 수준. 실제 컬럼/화면ID/API 경로/코딩규칙은 문서에 없어 신규 설계함 — 상세 구현 시 이 부분이 "추정"이었음을 감안할 것.
§ v2 완료(2026-07-05): PostgreSQL 지원(SQLAlchemy, DATABASE_URL 환경변수, schema_sqlite.sql/schema_postgres.sql 이중화), MES/WMS mock 연계(app/integrations.py + simulate_mes_wms.py), AI Agent 규칙기반 2종(Buyer 재발주추천/Scheduler 생산우선순위) 구현. 샌드박스에 root/Docker 권한이 없어 실제 PostgreSQL 서버 접속은 미검증 — 사용자가 로컬 docker-compose로 최종 확인 필요. AI Agent는 추천-실행(apply) API를 분리해 Human-in-the-loop 원칙 유지.
§ 샘플 데이터셋 임포트 완료(2026-07-05): prototype/prototype_dataset/(전자부품·소형가전 4개사/5공장/8창고, 17종 CSV)를 코드→ID 매핑 임포터(app/seed.py)로 반영. 기존 스키마에 code/external_no/warehouse_type/OEE 컬럼 및 demand_forecast/quality_inspection/shipment/finance_summary_monthly/kpi_monthly/ai_recommendation_log 6개 참조테이블 추가(전부 additive). 과거 거래는 재현하지 않고 재고스냅샷을 현재잔고로 직접 적재. 창고명 한글매칭 버그를 warehouse_type 컬럼 기반으로 수정.
§ 공장·창고 선택 UI 개선 완료(2026-07-05): [[dataset-warehouse-limitation]]에서 지적한 "항상 첫 매칭 창고 자동선택" 문제 해결. 출하/입고처리/생산실적입력 버튼에 인라인 드롭다운(공장명 포함 라벨, 확인/취소) 추가, 생산실적입력은 생산오더의 plant_id 기준으로 해당 공장 창고 우선정렬. 프론트엔드(static/index.html)만 변경, 백엔드 API 변경 없음. 브라우저 라이브 테스트로 3개 플로우 모두 확인.
§ AI Agent 5종 완료(2026-07-05): Buyer/Scheduler(v2)에 이어 Demand Planner(수요예측 MAPE 10%↑ 품목 재발주점/목표재고 조정 제안+적용), Quality Engineer(품질 리스크 추천, 조회전용), CFO Copilot(재무/현금흐름 인사이트, 조회전용) 구현 완료. 전부 규칙기반(LLM 미호출). Demand Planner apply는 승인 워크플로 없이 즉시 material 갱신(금액이동 없는 계획 파라미터라 판단), 적용 이력 미추적이 알려진 제약(반복 클릭 시 계속 상향 조정됨). eval-report-ai-agent-v3.md 참고.
§ PostgreSQL 로컬 검증 완료 — 프로토타입 v1~v2 전체 완료 확정(2026-07-05): 사용자 로컬 Docker(docker-compose)로 검증, 최종 전체 PASS(연결/스키마/시드/E2E). 과정에서 호스트 5432 포트가 기존 네이티브 Postgres에 점유되어 있던 문제 발견 → docker-compose.yml 호스트 포트를 5433으로 변경(향후 이 프로젝트의 Postgres 접속 정보는 항상 5433 기준). eval-report-postgres-verify.md(95/100) 참고. v2 eval부터 이어진 마지막 미검증 조건이 해소되어 v1~v2 스코프는 이걸로 완료. RBAC/LOT·Serial/LLM 고도화는 애초 v3 이후 스코프.
§ v3 스택 결정(2026-07-05): Spring Boot/Node.js 전환 요청에 현재 FastAPI 스택 유지를 권고 → 사용자 승인. 스택 전환은 MVP 완성 후 "Enterprise ERP" 단계에서 1회성 포팅으로 계획.
§ RBAC 구현 완료(2026-07-05, v3 우선순위 1): 세션 테이블 기반 인증(app/auth.py 신규, 표준 라이브러리 hashlib만 사용). 데모 계정 4개(관리자/영업담당/구매담당/생산담당, 비밀번호 공통 demo1234). 상태변경 POST만 역할 게이팅, GET은 명시적으로 비범위(내부망 전제). TestClient+브라우저 이중 검증, nav 중복 렌더링 버그 발견 즉시 수정. eval-report-rbac.md(92/100) 참고.
§ WMS/QMS LOT·Serial 추적 완료(2026-07-05, v3 우선순위 2): app/lot_tracking.py 신규(lot/lot_consumption/serial_number 테이블 additive). 기존 inventory 집계 테이블/adjust_inventory()는 그대로 두고 병행 레이어로 추가. 입고/생산실적/WMS IN → LOT 생성, 출하/생산 BOM 소요/WMS OUT → FIFO 소진. 시리얼은 선택기능(생산실적입력 시 generate_serials:true, 1회 최대 500개). QMS 실시간 검사 등록(POST /api/quality/inspections, 생산담당+관리자 게이팅 — "품질담당" 역할 없어 현실적 절충). 과거 임포트 재고는 LOT 소급 생성 안 함(비범위, FIFO 소진 시 가용 수량 부족해도 에러 없이 처리). 스키마 변경으로 사용자 라이브 erp.db 삭제 후 재기동 필요(RBAC 때와 동일 패턴). eval-report-lot-serial.md(91/100) 참고.
§ MES/WMS 웹훅 API Key 인증 완료(2026-07-05, v3 우선순위 3): 신규 테이블 integration_api_key(additive). X-API-Key 헤더 + source_system(MES/WMS)별 키 분리(교차 사용 차단), RBAC 세션 인증과는 별개 메커니즘. 데모 키 고정값(mes-demo-key-please-rotate / wms-demo-key-please-rotate, 세션 토큰처럼 평문 저장). GET /api/integrations/events는 계속 비인증. 부수 발견: simulate_mes_wms.py가 RBAC 이후(로그인 누락) + 데이터셋 임포트 이후(한글 창고명 매칭 실패) 이미 깨져 있었음 → 로그인 추가 + warehouse_type 기반 매칭으로 수정. 실제 uvicorn 백그라운드 기동이 이 샌드박스에서 불안정해(exit 143) TestClient로 동일 시퀀스 재현 검증, 사용자 로컬에서 최종 재확인 권장. eval-report-mes-auth.md(93/100) 참고.
§ AI Agent LLM 고도화 1단계 완료(2026-07-05, v3 우선순위 4, 마지막): 사용자에게 범위/방식 사전 확인 후 진행 — "자연어 근거 문장만 보강(추천 로직은 규칙기반 유지) + 템플릿 기반(실제 LLM API 호출 없음, 통합테스트 단계에서 필요시 API 연계)"로 확정. app/llm_rationale.py 신규(narrate_buyer/scheduler/demand_planner/quality/cfo, 표준 라이브러리만 사용). 5개 에이전트 응답에 `ai_narrative` 필드 additive 추가(기존 rationale/detail 필드 유지, 회귀 없음). 프론트 AI Agent 탭에 "AI 설명" 컬럼 추가. 스키마 변경 없음. eval-report-llm-narrative.md(94/100) 참고. 이걸로 사용자가 지시한 v3 순서(RBAC→LOT·Serial→MES인증→LLM고도화) 전부 완료.
§ v3 통합 재검증 + LOT 정합성 검증 로직 완료(2026-07-05): v3 4개 기능(RBAC/LOT·Serial/MES인증/LLM고도화)을 fresh 복사본에서 TestClient로 34개 항목 한 번에 재검증 — 전부 PASS, 회귀 없음 확인. 이어서 LOT·Serial 단계에서 알려진 리스크로 남겼던 "LOT 재고 ↔ inventory.qty 정합성 검증" 구현: `GET /api/lots/reconciliation`(app/lot_tracking.py 추가) — 완전 일치가 아니라 "활성 LOT 합계가 재고집계를 초과하는 경우만" 실제 버그로 판정(과거 임포트 재고로 인한 정상적 차이(미추적수량>0)는 오류 아님). 재고 탭에 정합성 점검 테이블 추가. 정상/버그 시뮬레이션 두 케이스 모두 TestClient로 검증, 스키마 변경 없음. eval-report-lot-reconciliation.md(95/100) 참고.
§ GET 인증 확장 + JWT 전환 + 시리얼 UI 고도화 완료(2026-07-05, v4): 진행 전 3개 세부 결정(GET 인증 범위/JWT 방식/시리얼 UI 범위) 사전 확인 — 전부 추천안(로그인만하면 전체조회, 세션테이블유지+JWT포맷, 출하자동반영+수동상태변경) 채택. (1) main.py 19개+ai_agent.py 5개+reference_data.py 6개+lot_tracking.py 5개+integrations.py 1개(GET /events) 총 36개 GET에 Depends(current_user) 추가 — `/`와 `/static/*`만 예외 유지. (2) app/auth.py에 표준 JWT(header.payload.signature, HMAC-SHA256) 자체구현(hmac/base64/json 표준라이브러리만, 신규 의존성 없음) — 세션 테이블은 유지해 여전히 DB 대조(로그아웃 즉시무효화 유지), JWT 서명검증은 DB조회 전 위조토큰 차단 방어선. JWT_SECRET 환경변수로 서명키 교체 가능. (3) consume_lots_fifo에 mark_serials_shipped 옵션 추가 — 출하(delivery) 시에만 True로 호출해 소진수량만큼 IN_STOCK 시리얼을 생성순으로 SHIPPED 자동반영. POST /api/serials/{no}/status 신규(IN_STOCK/SHIPPED/DEFECTIVE/SCRAPPED, 생산담당+관리자 게이팅) — 재고 탭에 상태변경 드롭다운 추가. 부수조치: simulate_mes_wms.py의 GET 호출들도 이제 로그인 토큰 필요해 auth_headers 추가. 26개 항목 통합 테스트 전부 PASS, 스키마 변경 없음(토큰 포맷/로직만 변경). eval-report-v4-auth-serial.md(93/100) 참고.
§ 운영 인프라 MVP 완료(2026-07-05, v4): 사용자 지시("운영 인프라(특히 마이그레이션 도구) 먼저") → 클리어링 질문에서 Alembic 도입 + 인프라 트랙 전체(마이그레이션+CI/CD+헬스체크/로깅) 한번에 진행 확정. (1) Alembic 도입 — 이 프로젝트의 첫 신규 런타임 의존성(사용자 확인). ORM 모델이 없어 autogenerate 불가 → migrations/versions/에 손수 raw SQL(op.execute) 마이그레이션만 작성. 0001_initial_schema.py는 기존 schema_sqlite.sql/schema_postgres.sql을 그대로 실행해 기존 스키마와 동일하게 재현. app/database.py의 init_db()가 3분기 자동판단: 신규DB→upgrade head, legacy DB(테이블 있음+alembic_version 없음)→stamp head(데이터 무손실, 더미행 보존으로 직접 검증), 이미추적DB→upgrade head. **이제 스키마 변경 시 erp.db 삭제가 필요 없음**(RBAC/LOT·Serial 때와 달라진 패턴 — 향후 스키마 변경은 migrations/versions/에 리비전만 추가). (2) GET /health(비인증, DB연결확인) + 표준 logging 모듈(신규 의존성 없음) 추가. (3) tests/ 디렉토리에 pytest 스위트 정식화 — 8개 파일(auth_rbac/jwt/lot_serial/lot_reconciliation/mes_wms_auth/ai_agent/serial_status/health), 42개 테스트 전부 PASS(/tmp 격리 SQLite, 실 erp.db 불변). requirements-dev.txt로 테스트 의존성(pytest) 분리. (4) .github/workflows/ci.yml — push/PR 시 pytest 자동실행. eval-report-mvp-infra.md(95/100) 참고. 다음 후보(v5): LLM API 실연계, GET 세부역할제한, 완전무상태 JWT, LOT정합성 자동알림, 시리얼 감사로그, 컨테이너화.
§ v5(GET 세부 역할 제한 + 완전 무상태 JWT + 컨테이너화) 완료(2026-07-05): 사용자 지시("GET 세부 역할별 제한, 완전 무상태 JWT, 컨테이너화 진행해") → 3개 항목 모두 AskUserQuestion 사전확인, 전부 추천안 채택. (1) GET 세부 제한 — 회계 전표(GET /api/accounting/documents*, /api/gl-accounts)와 CFO Copilot(GET /api/ai/cfo-copilot/insights) 4개만 current_user→require_roles(관리자)로 좁힘, 그 외 모듈은 v4와 동일(로그인만 하면 전체조회) 유지 — "실사용 피드백 없이 미리 잠그면 교차조회 UX 해친다"는 사용자 판단. 프론트: 회계 nav 탭을 비관리자에게 숨김, CFO Copilot 섹션은 API 호출 자체를 생략. (2) 완전 무상태 JWT — app/auth.py를 Access(JWT, TTL 30분, payload에 sub/name/email/roles 전부 담아 DB조회 없이 검증)+Refresh(기존 session 테이블 재사용, 스키마 변경 없음, TTL 8시간, 회전 안함) 이중토큰으로 재작성. 로그인 응답 {token}→{access_token,refresh_token,user}로 변경(하위호환 불필요, 배포전 프로토타입이라 프론트/테스트/시뮬레이터 전부 함께 갱신). 로그아웃은 refresh만 즉시삭제 — **이미 발급된 access token은 만료(최대 30분)까지 유효한 게 의도된 트레이드오프**(사용자가 AskUserQuestion에서 인지하고 선택, TTL을 짧게 유지해 창 최소화). 프론트 api() 헬퍼가 401 시 자동 refresh 후 원요청 재시도. (3) 컨테이너화 — Dockerfile(단일스테이지, python:3.11-slim, requirements.txt만 설치, psycopg2-binary라 빌드의존성 불필요) + docker-compose.yml에 app 서비스 추가(postgres healthcheck 통과 후 기동, DATABASE_URL 자동설정, 기존 GET /health를 컨테이너 healthcheck로 재활용). 이 세션 샌드박스에 Docker 데몬이 없어 실기동은 못했지만, 깨끗한 venv에 requirements.txt(Dockerfile과 동일)만 설치해 앱이 정상 기동+로그인+헬스체크됨을 확인해 간접검증. 신규 pytest 9개(GET제한 5+JWT refresh/logout 4) 추가해 51개 전부 PASS. **사용자가 이후 로컬에서 `docker compose up --build`로 실제 기동해 `GET /health`→`{"status":"ok","db":"ok"}` 확인 완료(2026-07-05)** — PostgreSQL 로컬검증(v2) 때와 동일 패턴으로 마지막 불확실성 해소. eval-report-v5.md(96/100, 최종검증 반영) 참고. 다음 후보(v6): LLM API 실연계, GET 세부제한 확대검토, refresh token 회전/다중세션, nginx/K8s, 부하테스트/외부로그수집.
§ v6(Refresh token 회전+재사용 탐지) 완료(2026-07-05): v5가 남긴 보안 절충("refresh token은 만료(8h)까지 재사용 가능")을 메움. app/auth.py에 rotate_tokens() 신규 — refresh 호출마다 새 access+refresh 토큰 발급, 기존 refresh token은 rotated_at 기록해 폐기(1회용). 이미 폐기된 토큰이 재사용되면 RefreshTokenReuseDetected 예외 → 그 로그인에서 파생된 전체 family_id 세션을 즉시 삭제(탈취 대응 표준 패턴 — 정상 클라이언트는 항상 최신 토큰만 쓰므로 옛 토큰의 재등장은 도난 신호). session 테이블에 family_id/rotated_at 컬럼 추가(migrations/versions/0002_session_rotation.py, Alembic). **주의사항으로 남길 설계 포인트**: schema_sqlite.sql/schema_postgres.sql은 절대 건드리지 않았음 — 0001 마이그레이션이 이 파일들을 실행 시점에 동적으로 읽으므로(baked-in 아님), 여기 손대면 신규 설치 시 0001이 이미 새 컬럼을 만들어버려 0002의 ADD COLUMN이 중복 오류를 낸다. **v4 이후 스키마 변경은 반드시 번호 붙은 마이그레이션 파일에만 남겨야 한다**(schema_*.sql 수정 금지, 이제 확립된 규칙으로 굳힘). 기존 세션은 family_id를 자기 token 값으로 백필해 개별 격리(빈 문자열 공유 시 무관한 세션들이 한 family로 묶여 동반 무효화되는 사고 위험 — 실제 Alembic 업그레이드 재현으로 백필 정상 동작 확인). family 만료는 로그인 시점 고정 8시간(회전해도 연장 안 됨). 로그아웃도 family 전체 삭제로 확장. 탈취 탐지 이벤트는 logging.warning만 남기고 audit_log 테이블 연동은 별도 과제로 이월. pytest 5개 신규(회전/재사용거부/family전체무효화/로그아웃 확장) 추가해 55개 전부 PASS. eval-report-refresh-rotation.md(94/100) 참고. 다음 후보(v7): LLM 실연계, 디바이스별 세션 관리 UI, audit_log 연동, nginx/K8s, 부하테스트.
§ UI 아이덴티티 정의 완료(2026-07-05): 사용자 지시로 /Users/greeme/Pictures/이미지_참조의 참조 이미지 16장(한국 회계ERP/보험청구/EduNova AI ERP/Acadexa 역할별 컬러 대시보드 등)을 분석해 ui-identity.md 작성 — 색상 토큰(브랜드 블루 #2F6FED, role badge 4색, success/warning/danger/info), 타이포/스페이싱/컴포넌트 규칙, 12개 실제 페이지별 적용 표. 명시적 비범위: 신규 기능/API 변경 없음, 다크모드 없음, 차트 라이브러리 교체 없음(레이아웃만 보완). mcp__visualize__show_widget으로 인터랙티브 목업 제시(위젯 자체 CDS 토큰 사용 — 실제 구현은 ui-identity.md의 hex값이 source of truth).
§ React 프론트엔드 마이그레이션 1차 완료(2026-07-05): "기능은 Python 유지, UI만 시각적/인터랙티브 스택으로 교체" 지시 → AskUserQuestion에서 "React+shadcn/ui+Tremor(추천)" 선택. 기존 FastAPI 백엔드가 이미 순수 JSON REST+JWT라 **백엔드 무변경**으로 SPA 교체 가능한 게 결정적 근거(HTMX 대비). frontend/ 신규(Vite+React19+TS+Tailwind v4 `@theme` 토큰에 ui-identity.md hex값 이식+React Router+TanStack Query). 패키지 대체 2건(사용자에게 이번에 처음 명시): `@tremor/react`는 Vercel 인수 후 배포방식 불확실해 미설치, 대신 recharts로 Tremor 스타일 컴포넌트 직접 구현 / shadcn CLI는 샌드박스 비대화형 프롬프트 행 위험 있어 컴포넌트 소스 직접 작성(shadcn 철학상 동일 결과). 1차 범위: 로그인+사이드바/상단바 셸(역할별 탭 숨김·배지)+대시보드(KPI 6종). 나머지 10탭은 PlaceholderPage, 2차~5차 순차 이관 예정. 검증: `npm run build` 성공(313KB 번들, 1.54s) + TestClient로 프론트 타입(User/LoginResponse/DashboardKpi)과 실 백엔드 응답 필드 완전 일치 확인. **사용자 로컬 브라우저 실기동 확인 완료(2026-07-05)**: 로그인→대시보드 KPI→placeholder 탭 안내까지 정상. 이 과정에서 로컬 환경 이슈 2건 발견/해결 — (1) frontend/ node_modules가 리눅스 샌드박스에서 처음 npm install돼 rolldown 네이티브 바이너리가 리눅스용으로 박혀 Mac에서 기동 실패 → node_modules/package-lock.json 삭제 후 Mac에서 재설치로 해결. (2) **IPv4/IPv6 프록시 혼선(재발 방지 위해 기억)**: 8000번 포트에 v5 검증 때 쓴 Docker 컨테이너(IPv6 `*:8000`)와 새 uvicorn(IPv4 `127.0.0.1:8000`)이 동시에 떠 있었는데, vite.config.ts 프록시 대상이 `localhost`라 Mac에서 IPv6로 먼저 풀려 낡은 Docker 컨테이너로 요청이 새 로그인 500 에러 발생 — `docker compose down`+프록시 대상을 `127.0.0.1`로 고정해 해결. **향후 이 프로젝트에서 로컬 프론트 개발 중 원인불명 500/네트워크 에러가 나면 `docker compose ps`로 잔여 컨테이너부터 의심할 것**. eval-report-frontend-v1.md(93/100) 참고.
§ React 프론트엔드 2차(기준정보/영업/구매) 완료(2026-07-05): 사용자 지시("이관 진행")로 진행. 공용 컴포넌트 3종 신규(LineItemsEditor: SO/PR/PO 라인아이템 편집기, WarehousePicker: 행 액션 인라인 창고선택(공장 우선정렬)+확인/취소 — 기존 static/index.html의 openWarehousePicker 패턴 재현, StatusBadge: 상태값 전체를 ui-identity.md semantic 매핑으로 통일). components/ui/select.tsx는 Radix 대신 네이티브 select 스타일링만 사용(과설계 회피). MdmPage/SalesPage/ProcurementPage 3개 페이지, 정보항목은 기존과 완전 동일 유지. **샌드박스 npm 플랫폼 문제 주의**: 이 세션(리눅스)과 사용자 Mac이 같은 mount를 공유해 node_modules를 한쪽에서 설치하면 반대쪽에서 rolldown 네이티브 바이너리를 못 찾는 현상이 반복됨(1차 때는 리눅스→Mac, 이번엔 Mac→리눅스) — **앞으로 이 프로젝트의 frontend/ 빌드 검증은 `npx tsc -b`(타입체크, 플랫폼 무관)까지만 샌드박스에서 하고, `vite build`/`npm run dev`는 항상 사용자 로컬에서 최종 확인하는 것으로 고정**. 검증은 tsc 타입체크(에러 없음)+TestClient로 8개 신규 연동 엔드포인트(materials/customers/vendors/warehouses/plants/sales-orders/purchase-requisitions/purchase-orders) 응답 필드 전체 일치 확인 + SO 등록→출하→청구, PR→PO→입고처리 전체 흐름 실제 호출 검증. eval-report-frontend-v2.md(90/100, 브라우저 실기동만 미확인) 참고. 다음: 3차(생산/재고)~5차(AI Agent/참고데이터) 이관, 사용자 로컬 브라우저 확인.
§ React 프론트엔드 3차(생산/재고) 완료(2026-07-05): 사용자 지시("3차(생산/재고)로 진행"). ProductionResultPicker 컴포넌트 신규(수량+창고+시리얼생성 체크박스, WarehousePicker와 별개 — 기존 openResultPicker 패턴 재현). ProductionPage(생산오더 등록/작업지시/실적입력 + QMS 품질검사 등록, 오더별 최근 work_order_id는 React state로 보관해 기존 전역 LAST_WORK_ORDER 패턴 재현), InventoryPage(현재고/이동이력/LOT추적/시리얼추적(상태변경, 생산담당+관리자만)/LOT정합성 점검 — 부정합 행은 배경 danger 톤 강조). LOT/시리얼 추적은 기존과 동일하게 요약 메시지로 표시(펼침형 상세는 후속 폴리시 후보로 남김). 검증: tsc -b 에러 없음 + TestClient로 생산오더 등록→작업지시→실적입력(시리얼 5건 생성 확인)→품질검사 등록 전체 흐름 실제 호출, 재고/LOT/시리얼/정합성 6개 엔드포인트 응답 필드 전부 프론트 타입과 일치 확인. eval-report-frontend-v3.md(90/100, vite build/브라우저 실기동만 미확인) 참고. **패턴 확정**: 이 프로젝트의 frontend 각 단계 검증은 항상 tsc -b(타입)+TestClient(API 계약)로 여기서 하고, vite build/브라우저 확인은 사용자 로컬 몫 — 플랫폼(리눅스 샌드박스↔Mac) 바이너리 공유 문제로 이 세션에서 vite 자체를 못 돌리기 때문(1~3차 공통).
§ React 프론트엔드 4차(회계/승인함/연동로그) 완료(2026-07-05): 사용자 지시("4차 진행"). FinancePage(관리자 전용, hasRole 아니면 API 호출 자체 생략+안내문구 — 백엔드 require_roles(관리자) 403과 실제로 일치 확인), ApprovalsPage(목록+PENDING건 승인/반려, Button success/danger variant 활용), IntegrationsPage(MES/WMS 아이콘 구분+payload 보기 토글로 축약). 검증 중 흥미로운 점: 프레시 DB는 시드만으로 accounting_document/approval_workflow/integration_event_log 3개 테이블이 전부 비어 있어(트랜잭션으로만 채워지는 테이블) — 수동전표 등록/PR 등록/WMS 웹훅을 직접 호출해 실제 행을 만든 뒤 재검증하는 방식으로 API 계약 확인(tsc -b 통과 + 필드 전체 일치 + 비관리자 403 실제 확인). eval-report-frontend-v4.md(91/100, vite build/브라우저 실기동만 미확인) 참고. 다음: 5차(AI Agent/참고데이터) 이관 → `static/index.html` 제거 여부 결정, 1~4차 누적분 사용자 로컬 브라우저 확인.
§ React 프론트엔드 5차(AI Agent/참고데이터) 완료 — **1~5차 전체 이관 완료**(2026-07-05): 사용자 지시("5차 진행"). AiAgentPage(5개 에이전트 카드, 신규 AiNarrative 컴포넌트로 rationale(규칙기반)과 ai_narrative(AI서술) 시각적 분리, CFO Copilot은 v5 정책대로 관리자 아니면 API 호출 자체 생략), ReferenceDataPage(6개 서브테이블을 세로나열 대신 탭으로 전환, ui-identity.md 권고 반영). 더 이상 어떤 라우트에서도 참조되지 않는 PlaceholderPage.tsx 삭제(CLAUDE.md Surgical Changes 원칙 — 자기 변경으로 발생한 미사용 코드 정리). 검증: tsc -b 에러 없음 + TestClient로 AI Agent 5종(scheduler 58건/demand 1건/quality 5건/cfo 4건 실데이터, buyer는 시드 상태상 재발주 대상 없어 0건이라 소스코드 직접 대조로 확인)+참고데이터 6종(kpi 12/fin 12/forecast 60/shipments 124/qi 180/ailog 5건) 응답 필드 확인, 비관리자 CFO 조회 403 재확인. eval-report-frontend-v5.md(91/100, vite build/브라우저 실기동만 미확인) 참고.
§ static/index.html 제거 완료(2026-07-05, v7 마무리): AskUserQuestion으로 확인 후 사용자가 "지금 제거" 선택. (1) static/index.html·static/ 디렉토리 삭제. (2) frontend/dist/도 함께 삭제 — 1차 단계에서만 빌드된 스테일 아티팩트(313KB 번들, 로그인+대시보드만 포함)라 그대로 두면 "/"에서 옛날 화면이 서빙돼 2~5차 페이지가 없는 것처럼 혼동을 줄 위험이 있었음. (3) app/main.py: STATIC_DIR→FRONTEND_DIST_DIR로 교체, `/`는 frontend/dist/index.html이 있으면 서빙하고 없으면 `npm run dev`/`npm run build` 안내 JSON 반환(항상 200 — 인증 불필요 유지), `/static` mount는 `/assets`(Vite 빌드 출력 규격)로 교체하되 frontend/dist 존재할 때만 마운트(존재하지 않는 디렉토리를 mount하면 앱이 기동 자체를 못 함). (4) Dockerfile의 `COPY static ./static` 제거(안 그러면 빌드 실패) — 프론트엔드를 이미지에 포함하는 건 Node 멀티스테이지 빌드가 필요해 별도 과제로 이월(주석으로 남김). 검증: pytest 55개 전부 회귀 없이 통과(GET "/" 200 확인하는 test_health.py::test_index_static_no_auth_required, test_auth_rbac.py::test_static_root_no_auth_required 포함 — 응답 바디가 파일→JSON으로 바뀌었지만 두 테스트 모두 status_code만 확인해 그대로 통과). **이걸로 React 프론트엔드 마이그레이션(1~5차+마무리)이 완전히 종료됨.** 남은 것은 2~5차 페이지의 사용자 로컬 브라우저 실기동 확인뿐(1차만 확인 완료).
§ 사이드바 접기/펼치기(ChatGPT 스타일) 추가(2026-07-05): AppShell.tsx에 `collapsed` state(localStorage `erp_sidebar_collapsed`로 새로고침 후에도 유지) + 토글 버튼(PanelLeftClose/PanelLeftOpen 아이콘) 추가. 펼침: 사이드바 224px(w-56)에 아이콘+앱이름+메뉴텍스트 전부 표시. 접힘: 64px(w-16) 아이콘 레일만, 텍스트는 `hidden`, 각 NavLink에 `title` 속성 부여로 hover 시 메뉴명 확인 가능. 너비 전환은 `transition-all duration-200 ease-in-out`. **모바일(md 미만)은 토글 상태와 무관하게 항상 w-16 아이콘 레일로 고정**(토글 버튼 자체도 `md:` 이상에서만 노출) — 좁은 화면에서 사이드바가 폭을 과점해 본문이 깨지는 문제를 방지(사용자 요청 "모바일에서는 1열 기준으로 보정"). main 영역 패딩도 `p-4 md:p-6`로 반응형 조정. tsc -b 통과 확인. 순수 프론트엔드 변경(백엔드/API 무관)이라 별도 eval-report 없이 메모리에만 기록.
§ 1.0 메뉴 구조 전체 구현 로드맵 수립(2026-07-05): 사용자 지시 "1.0_AX_ERP_Menu_Structure의 모든 메뉴 기능을 순차적으로 구현하기 위해 1.1부터 md 문서들을 순서대로 읽어서 계획을 수립하고 확인하면 진행"에 따라 프로젝트 루트의 1.0~5.8 md 문서 전부(약 30개)를 조사. 결론: 1.0(461줄, 13개 모듈·153개 리프 메뉴)만 유일하게 상세하고, 1.1~1.7은 각 10~20줄짜리 개요, 2.1/2.5~2.7은 기능ID 목록+마스터플랜 5단계 정도이며, 3.x/4.x/5.4~5.8 상당수는 Multi-Company/Multi-GAAP/Kubernetes/Vector DB/300+KPI/500+SoD 같은 "목표 규모"만 선언한 Enterprise/AX 단계 스켈레톤(실제 실행 가능한 스펙이 아님, 프로토타입_가이드.md의 Prototype→MVP→Enterprise ERP→AX Platform→Autonomous Enterprise 사다리에서 몇 단계 위). 현재 프로토타입(65개 엔드포인트/11개 프론트 페이지) 대비 153개 항목 중 29개 구현+14개 부분구현(19%), 09 Controlling/10 Marketing/11 Service는 전무. `task-plan-v9-full-menu-rollout.md`에 메뉴 01→13 순서 그대로 13개 웨이브(v9~v21) 로드맵 작성, 스코프 원칙(기존 FastAPI+React 스택 유지, additive 마이그레이션만, 프로토타입 티어로 단순화 vs Enterprise 단계 이연 구분)도 명시. 사용자 확인 결과: (1) 자동 순차 진행(웨이브마다 검증+eval-report 후 바로 다음 진행, 매번 재확인 안 받음), (2) v10 SCM의 "공급망 시뮬레이션"은 이번 로드맵에서 제외, (3) v20 MDM의 "코드관리"는 기존 하드코딩 리팩터 없이 신규 기능만 추가. [[v9-sales-extension]]
§ v9(01 Sales Management 확장) 완료(2026-07-05): task-plan-v9-full-menu-rollout.md §3 v9. 마이그레이션 0004(additive) — price_policy/quotation+quotation_line/sales_contract/sales_return+sales_return_line/service_order 신규 테이블 + material.std_cost 컬럼(손익 근사용). 신규 라우터 app/sales_ext.py — 가격정책(고객전용 우선 lookup), 견적(등록 시 즉시 approval_workflow 등록 → 승인함에서 승인 → 승인된 것만 수주 전환 가능), 판매계약, 반품(승인 시에만 adjust_inventory로 재고복원+audit_log, 반려 시 재고 불변), 서비스오더(접수+상태변경), 채권조회(payment_term "NET30"에서 숫자 추출해 만기 근사+연체표시), 영업실적/손익분석(고객·제품·월별 그룹, 손익은 매출-qty×material.std_cost 단순근사 — 정교한 COPA는 v17로 이연), 영업KPI. **핵심 설계 결정**: 견적/반품 승인은 신규 UI 없이 기존 approval_workflow(PR/PO와 동일 테이블)에 등록해 기존 승인함 화면을 그대로 재사용 — app/main.py의 decide_approval()에 QUOTATION/SALES_RETURN 분기만 추가. SalesPage.tsx를 탭 7개(수주/견적/가격정책/판매계약/반품/서비스오더/영업분석)로 재구성. 테스트: 신규 tests/test_sales_extension.py 11개(견적 승인전 전환차단, 반품 승인시 재고+2/반려시 불변 등) 추가, 기존62+신규11=73개 전부 PASS, tsc -b 통과. **재발방지 메모**: (1) 프로토타입 전체(`cp -r prototype/`, node_modules 등 포함)를 /tmp에 복사하면 이 세션 샌드박스에서 45초 타임아웃 나는 경우가 생김 — 이후로는 `cp -r app migrations tests requirements*.txt alembic.ini prototype_dataset /tmp/이름/`처럼 백엔드 관련 디렉토리만 선택 복사할 것(frontend/node_modules·.venv·frontend/dist 등 대용량 디렉토리 제외). (2) GET /api/inventory는 필터 쿼리파라미터가 없어 항상 전체 목록을 반환한다(material_id/warehouse_id로 필터링되지 않음) — 테스트나 프론트에서 특정 행이 필요하면 클라이언트 사이드에서 골라내야 함. eval-report-v9.md(91/100) 참고. 다음: v10(02 SCM 신규).
§ v10(02 SCM 신규) 완료(2026-07-05): task-plan-v9-full-menu-rollout.md §3 v10. 신규 테이블 없이 기존 demand_forecast/production_order/inventory/purchase_order/goods_receipt/vendor/material을 집계하는 조회전용(GET) API만 추가 — app/scm.py(prefix /api/scm): 수요예측정확도(품목별 평균MAPE+방향성), S&OP(수요예측 vs 생산계획 월별 대조), 공급계획(현재고+미입고PO), MPS뷰(생산오더 월별 재구성), 재고계획(재주문점/목표재고 대비 현재고 갭), 공급위험관리(공급사별 납기지연율), Control Tower(위 지표 요약, 각 엔드포인트 함수를 직접 호출해 재사용). **버그 발견/수정**: demand_forecast.forecast_month가 "2026-12-01"(전체날짜) 형식으로 저장되는데 생산계획 쪽은 substr(order_date,1,7)="2026-12"(월만)로 그룹화해 S&OP 대조가 항상 planned_qty=0이 되는 버그를 수동검증 중 발견 — forecast_month도 동일하게 substr(,1,7) 처리해 수정, 회귀 테스트(tests/test_scm.py::test_sop_gap_matches_same_month_period_format)로 재발방지. 공급위험관리는 샘플 데이터셋이 재고를 스냅샷으로 직접 적재해(구매→입고 트랜잭션 이력 없음) 빈 배열 반환이 정상 — 프론트에 안내문구 추가. 프론트: 신규 ScmPage.tsx(7탭) + App.tsx/AppShell.tsx에 /scm 라우트·사이드바(Radar 아이콘) 추가. 사용자 확인에 따라 **공급망 시뮬레이션은 이번 로드맵 전체에서 제외**. 테스트: tests/test_scm.py 8개 추가, 기존73+신규8=81개 전부 PASS, tsc -b 통과. eval-report-v10.md(92/100) 참고. 다음: v11(03 Procurement 확장).
§ v8(Docker 프론트엔드 포함 + audit_log 연결 + LOT 정합성 KPI + 세션 관리) 완료(2026-07-05): 사용자 지시("2순위 — 프로덕션 배포 마무리, 3순위 — 남겨둔 보안/운영 절충점 채우기 진행해"). (1) Dockerfile을 2-스테이지(node:20-slim으로 frontend 빌드→python:3.11-slim에 dist만 복사)로 전환 — Stage 1은 package-lock.json을 복사하지 않고 package.json만으로 npm install(이 프로젝트에서 두 번 겪은 rolldown/vite 네이티브 바이너리 플랫폼고정 버그를 원천 차단). app/main.py는 v7에서 이미 frontend/dist 자동감지 로직을 넣어둬 코드 변경 불필요. **샌드박스에 Docker 없어 실빌드 미검증**(사용자 로컬 확인 필요, v5 컨테이너화 때와 동일 제약). (2) app/helpers.py에 write_audit_log() 신규 — v1부터 있었지만 아무 코드도 쓰지 않던 audit_log 테이블을 처음 사용. 시리얼 상태변경(update_serial_status)과 refresh token 재사용 탐지(RefreshTokenReuseDetected) 이벤트를 기록, 상태전이는 스키마 변경 없이 action 문자열에 "STATUS_CHANGE:A->B"로 인코딩. GET /api/audit-log(관리자 전용) 신규. (3) app/lot_tracking.py에 count_lot_inconsistencies(conn) 집계함수 분리(기존 GET /lots/reconciliation의 판정로직 재사용, RECONCILIATION_EPSILON 상수 공유) → GET /api/dashboard/kpi에 lot_inconsistent_count 추가, DashboardPage에 3번째 KPI 카드로 노출(0건=success/1건이상=danger 톤). 새 스케줄러/의존성 없이 기존 폴링 KPI에 편승. (4) migrations/versions/0003_session_device_info.py — session 테이블에 user_agent/last_seen_at 추가(0002와 동일하게 schema_*.sql은 건드리지 않음, 기존 행은 last_seen_at←created_at 백필). app/auth.py의 create_refresh_token/issue_tokens/rotate_tokens가 user_agent를 받아 기록(회전 시엔 원래 세션의 user_agent 유지). GET /api/auth/sessions(rotated_at IS NULL인 현재 유효 세션만, 본인 것만) + POST /api/auth/sessions/{family_id}/logout(소유권 검증 — family_id의 user_id가 요청자와 다르면 404, 검증 없으면 family_id 추측만으로 남의 세션 강제로그아웃 가능해짐) 신규. 프론트: components/ui/dialog.tsx(@radix-ui/react-dialog 최소 래퍼 — 설치만 되어있고 미사용이던 의존성 최초 사용) + SessionsDialog.tsx(topbar 모니터 아이콘→세션 목록+기기별 로그아웃), AppShell.tsx topbar에 삽입. 검증: 신규 pytest 7개(tests/test_v8_sessions_audit.py — 감사로그 2, 관리자전용 1, KPI필드 1, 세션조회/로그아웃/소유권검증 3) 추가해 기존 55개+신규7개=62개 전부 PASS(fresh /tmp 복사본), tsc -b 에러 없음. eval-report-v8.md(90/100, Docker실빌드+신규UI 브라우저실기동만 미확인) 참고. 다음 후보(v9): React 전체+v8 신규 UI 브라우저 실기동 확인, Docker 실빌드 검증, audit_log 조회 프론트 화면, LLM 실연계, GET 세부역할 확대, migrations/0003 PostgreSQL 실환경 검증, nginx/K8s/부하테스트.
§ Sub-Agent 체계 + Hermes 폐쇄형 학습 루프 적용(2026-07-07): 사용자 지시로 (1) `/Users/greeme/Claude` md 기반 Sub-agent 체계, (2) hermes-cowork closed-loop 기법 적용 두 가지만 수행. `/Users/greeme/Claude` 최상위는 보호 경로 충돌로 마운트 불가(재확인) — 이미 프로젝트 루트에 있는 5개 파일(Harness/Planner/Executor/Evaluator/Memory_CLAUDE.md, 2026-07-05 최초 조사분과 동일)을 유효 소스로 사용. hermes-cowork는 하드 문자한도(2,200/1,375자) 템플릿이라 이미 그 한도를 훨씬 초과한 이 프로젝트의 memory/MEMORY.md·USER.md에는 그대로 복사하지 않고, 메커니즘(자동 저장 트리거·세션로그·스킬추적·curator 주기)만 CLAUDE.md 하단에 경로 매핑해 추가. `memory/SKILLS_USAGE.json` 신규 생성. `memory/SESSION_LOG.md`는 이미 2026-07-05부터 운영 중이던 파일로 확인(신규 아님) — 신규 세션 항목만 추가.
§ v11(03 Procurement Management 확장) 완료(2026-07-07): task-plan-v9-full-menu-rollout.md §3 v11. 마이그레이션 0005(additive) — vendor_evaluation(납기/품질/가격 100점 만점)/purchase_contract(sales_contract와 동일 패턴)/import_customs_record(PO단위 간이 통관기록) 신규 테이블 + purchase_order.po_type(STANDARD/OUTSOURCING/CONSIGNMENT) 컬럼. 신규 라우터 app/procurement_ext.py — 공급업체평가 등록+평균점수/등급(A≥80/B≥60/C미만) 요약, 구매계약 등록/조회, 카테고리별 구매(material.material_type 필터 뷰, 신규 테이블 없음 — **알려진 범위 제한**: 현재 시드 데이터는 material_type이 RM/FG만 존재해 부자재/설비/금형은 데이터 등록 전까지 노출 안 됨, API는 이미 대응), 통관 등록/상태변경(PENDING→DECLARED→CLEARED/HOLD), 구매실적(vendor/material/month), 구매KPI. **핵심 설계 결정**: po_type은 v9의 quotation/sales_return과 달리 승인 워크플로가 필요 없는 단순 분류라 app/main.py의 create_po()에 파라미터 1개만 추가(기존 PR/PO/입고 로직 무변경) — procurement_ext.py는 순수 신규 엔드포인트만 담당. ProcurementPage.tsx를 탭 6개(발주관리/공급업체평가/구매계약/카테고리별구매/통관관리/구매분석)로 재구성. 테스트: 신규 tests/test_procurement_extension.py 12개(평가등록+요약+미존재공급사404, 계약등록, 카테고리shape+필터, po_type기본값/명시값/잘못된값400, 통관등록+상태변경+잘못된상태400+미존재PO404, 실적3종group_by, KPIshape, 로그인필요) 추가, 기존81+신규12=93개 전부 PASS, tsc -b 통과. eval-report-v11.md(91/100) 참고. 다음: v12(04 Logistics Management 확장).
§ v12(04 Logistics Management 확장) 완료(2026-07-11): task-plan-v9-full-menu-rollout.md §3 v12. 마이그레이션 0006(additive) — warehouse_location/container/shipment_transport/logistics_cost/insurance_policy/logistics_claim 6개 테이블 + gl_account "5100 물류비" 1행(첫 데이터 시드 포함 마이그레이션). 신규 라우터 app/logistics.py(17개 엔드포인트) — Location 마스터(inventory와의 바인딩은 Enterprise 이연), 컨테이너(EMPTY→…→RETURNED), 운송/배차(TMS 간이), 물류비 정산(post_accounting 재사용, 차변5100/대변2000 — 정산은 관리자 전용, v5 회계정책과 일치), 수출현황(shipment 재구성 조회), 보험/클레임, 물류 Dashboard. 프론트 LogisticsPage.tsx 7탭 + /logistics 라우트(Ship 아이콘). 날짜는 전부 date.today() 명시(같은 날 수정한 UTC 밀림 재발방지). 테스트 18개 추가(93→111 전부 PASS, Mac venv), tsc -b 통과, 브라우저 E2E(배차등록→물류비 정산→전표#1 생성) 확인. eval-report-v12.md(92/100). 같은 날 선행 수정 3건: SPA fallback(catch-all, api/assets/health 제외), 통관 po_no(external_no) 입력 허용, eval_date/customs_date 로컬날짜 명시. 다음: v13(05 Production 확장 — MRP/외주/재작업/생산마감/OEE).
§ v13(05 Production Management 확장) 완료(2026-07-11): task-plan §3 v13. 마이그레이션 0007(additive) — production_order.is_outsourced/vendor_id + rework_order/production_close. app/production_ext.py(9개 API): MRP(최신 수요예측월×BOM 전개, 단일레벨, 조회전용 — AI Buyer와 별개 로직임을 UI 명시), 외주생산 현황, 재작업(DEFECTIVE 시리얼만, REWORKED→IN_STOCK/SCRAPPED, audit_log 기록, 신규 시리얼 상태 미도입), 생산마감(양품×std_cost 근사 전표 차변1200/대변5000, 마감월 실적입력 잠금 — main.py assert_period_not_closed 1곳), OEE 실측 집계(월×공장, dataset 실측 승격 + kpi_monthly 참고 병행), 생산 Dashboard. main.py 최소수정 3곳(외주 파라미터/vendor join/마감체크). ProductionPage 7탭 재구성(기존 오더/QMS 로직 그대로 보존). 테스트 15개 추가(111→126 PASS), tsc 통과, 브라우저 검증(MRP 2026-12 10개 자재/OEE 월×공장/현황 KPI 7종). 마감 잠금 테스트는 당월 마감→400 확인→db_path로 마감행 직접 삭제(후속 테스트 보호 패턴). eval-report-v13.md(92/100). 다음: v14(06 Quality 확장).
§ v14(06 Quality Management 확장) 완료(2026-07-11): task-plan §3 v14. 마이그레이션 0008(additive) — inspection_standard/eight_d_report/customer_claim/capa_action. app/quality_ext.py(14개 API): 검사기준(LSL/USL=Cp·Cpk 기준), 검사이력 필터(inspection_type — v1부터 있던 컬럼 재사용, 신규컬럼 없음), SPC/공정능력(defect_ppm 근사 p-관리도 + 검사기준 있을 때만 Cp/Cpk — 측정치 실측 SPC는 Enterprise 이연), 부적합(DEFECTIVE 시리얼+FAIL 검사 통합조회, 처리는 v13 재작업), 8D 간이(D2/D4/D5), 고객클레임(등록은 영업담당도 허용 — 고객접점 예외), CAPA(capa_required=Y 미조치 검사를 후보로 자동노출→등록 시 연결·제외), 품질 Dashboard. QualityPage 7탭 신설(/quality, ShieldCheck). 테스트 14개 추가(126→140 PASS — SPC 테스트는 검사데이터 있는 품목 동적선택으로 1회 수정), tsc 통과, 브라우저 검증(KPI 180건/PPM 1444.2/CAPA후보 89건, SPC 실계산). **알려진 제약**: 시드 검사데이터의 inspection_type이 "OUTGOING" 등 자유문자열이라 신규 enum(INCOMING/IN_PROCESS/FINAL) 필터에 안 잡힘 — 시드 정규화는 이월. eval-report-v14.md(91/100). 다음: v15(07 Engineering/R&D 신규).
